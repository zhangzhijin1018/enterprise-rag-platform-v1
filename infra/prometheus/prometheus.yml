# Prometheus 抓取配置。
# 当前只抓本项目 API 的 `/metrics` 指标。

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: enterprise-rag-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
